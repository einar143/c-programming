presented tasks 09-15:
{1.1.1, 1.1.2, 1.1.3, 1.2.1, 1.2.2, 1.3.4, 1.3.7}
Total points: 8

completed code:


Do these
Task	Points	Section	Why it's worth your time
1.5.11 Säker kopiering	2	1.5	Write your own SafeCopyString without strncpy. It's about buffer overflows, so you have to think through the edge cases: a NULL pointer, count of 0, a string with no terminating zero, overlapping memory. This is the most real-world task in the course.
2.1.7 Sorting (isort)	2	2.1	Write an insertion sort with the same signature as qsort: void* input, pointer math through char*, memcpy, and a comparison function passed in. This is how C does generic code.
3.2.1 Dynamisk länkning	2	3.2	Build two DLLs yourself and call them from one program. It's short to code, but you learn how the build and linker actually work.
1.6.2 Binärträd	2	1.6	Nodes allocated with malloc and linked by parent/left/right pointers, a print function (recursion, and you can make the output look nice), and freeing everything cleanly at the end.
1.4.4 Bit operationer	1	1.4	Rotate the bits of an unsigned int left. It's a small puzzle: (x << y) | (x >> (bits - y)), plus handling y >= 32.
3.1.1 Statisk länkning (teori)	1	3.1	A text file on file size, performance and portability. You'll learn this anyway while doing 3.2.1, so it's a nearly free point.